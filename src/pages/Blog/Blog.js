import React from 'react';
import Layout from '../../components/Layout/Layout';

const Blog = () => {
    return (
        <Layout title='Blog'>
            <h1>Blog</h1>
        </Layout>
    );
};

export default Blog;